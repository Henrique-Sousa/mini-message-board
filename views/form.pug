extends layout

block content
    h1= title
    form(method='post' action='/new')
        div 
            label(for='message_user') Author's name: 
            input(type='text', name='message_user')
        div 
            label(for='message_text') Message text: 
            input(type='text', name='message_text')
        button(type='submit') Publish